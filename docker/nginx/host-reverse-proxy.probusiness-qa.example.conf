# QA: intranetback-qa.probusiness.pe — Nginx en el HOST → Docker
#
# Instalar en el servidor:
#   sudo cp docker/nginx/host-reverse-proxy.probusiness-qa.example.conf \
#          /etc/nginx/conf.d/intranetback-qa.probusiness.pe.conf
#   sudo certbot certonly --nginx -d intranetback-qa.probusiness.pe
#   sudo nginx -t && sudo systemctl reload nginx
#
# En /var/www/html/intranet_back_qa/.env:
#   APP_ENV=qa
#   APP_URL=https://intranetback-qa.probusiness.pe
#   APP_PORT=8085
#   COMPOSE_PROJECT_NAME=intranet_qa
#   HORIZON_PREFIX=qa_horizon:
#   REDIS_PREFIX=qa_
#   WHATSAPP_USE_DEFAULT_NUMBER=true

server {
    if ($host = intranetback-qa.probusiness.pe) {
        return 301 https://$host$request_uri;
    }
    listen 80;
    server_name intranetback-qa.probusiness.pe;
}

server {
    listen 443 ssl http2;
    server_name intranetback-qa.probusiness.pe;

    client_max_body_size 700M;
    client_body_timeout 3000s;
    client_header_timeout 300s;

    error_log /var/log/nginx/intranetback_qa_error.log;
    access_log /var/log/nginx/intranetback_qa_access.log;
    ssl_certificate /etc/letsencrypt/live/intranetback-qa.probusiness.pe/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/intranetback-qa.probusiness.pe/privkey.pem;

    # --- API Laravel (clone QA en Docker, puerto 8080) ---
    location / {
        proxy_pass http://127.0.0.1:8085;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 1800s;
        proxy_send_timeout 1800s;
        proxy_read_timeout 1800s;
    }

    location /api/bitrix/webhook {
        proxy_pass http://127.0.0.1:8085;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Horizon dashboard (opcional restringir por IP en QA)
    location /horizon {
        proxy_pass http://127.0.0.1:8085;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # --- WebSockets QA: puerto distinto al de PROD (6002 vs 6001) ---
    # Levantar websockets del clone QA, ej. en intranet_back_qa:
    #   php artisan websockets:serve  (o supervisor dedicado en 6002)
    # Cuando dockerice websockets, cambiar a proxy al contenedor correspondiente.
    location /app/ {
        proxy_pass http://127.0.0.1:6002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }

    location /laravel-websockets {
        proxy_pass http://127.0.0.1:6002;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
